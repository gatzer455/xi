# Agent / LLM dev context (workspace, not part of the project)
AGENTS.md
CONTEXT.md
.pi/
.develop/

# Backend (Rust / Cargo)
backend/target/

# Sidecar pi compilado + su package.json (regenerado por build-pi.sh)
backend/binaries/
backend/pi-x86_64-unknown-linux-gnu
backend/pi-sessions-x86_64-unknown-linux-gnu
backend/.git/        # sub-repo accidental que crea tauri/cargo
backend/.gitignore   # lo ignora el .gitignore raíz

# Frontend (Node / Vite)
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Editors
.vscode/
.idea/
*.iml
*.swp
*.swo

# OS junk
.DS_Store
Thumbs.db
*~

# Env / secrets
.env
.env.local
.env.*.local

# Tauri updater signing keys (NUNCA commitear la private key)
# La pubkey (xi.key.pub) va EMBEBIDA en tauri.conf.json como string,
# no como archivo. La private key (xi.key) queda en ~/.tauri/ y en
# GitHub Secrets. Si algún día la key se mueve al repo, ignoramos.
xi.key
xi.key.pub

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
docs/pi-approve-diagnosis.md
